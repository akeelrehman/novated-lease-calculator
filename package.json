{
  "name": "novated-lease-calculator",
  "version": "2.1.0",
  "description": "Novated lease calculator — Australia 2026",
  "scripts": {
    "build": "node build.js",
    "watch": "node watch.js",
    "test": "node --test tests/calculator.test.js",
    "test:e2e": "npx playwright test",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
